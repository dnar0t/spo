{
  "name": "@spo/shared",
  "version": "1.0.0",
  "description": "Shared types, enums, constants and utilities for СПО",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
