interface SyncDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
}
export declare function SyncDialog({ open, onOpenChange }: SyncDialogProps): import("react").JSX.Element;
export {};
